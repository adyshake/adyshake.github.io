adyshake.github.io
==================

Eleventy site. The only npm dependency is `@11ty/eleventy`.

```bash
npm install
npm run serve
```

GitHub Pages serves the `master` branch. Publish a build with:

```bash
npm run deploy
```
